package Static;

public class BaseDefinition {
    //The static keyword is used to define class-level variables and methods — meaning they belong to the class, not to instances (objects).
    //Use of static	            Description
    //Static Variable (Field)	Shared by all instances of a class
    //Static Method	            Can be called without creating an object
    //Static Block	            Executes once when the class is loaded
    //Static Class (nested)	    Inner class that doesn't access outer class instance members
}
